[build]
  command = "npm ci && npm run build:all"
  publish = "."

# Run an edge guard on share/ and og/ before anything else
[[edge_functions]]
path = "/share/*"
function = "future-guard"

[[edge_functions]]
path = "/og/*"
function = "future-guard"
